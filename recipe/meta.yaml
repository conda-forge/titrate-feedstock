{% set name = "titrate" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/titrate-{{ version }}.tar.gz
  sha256: 8d2ca83c6c2bf5fbc1db147a14c5c18a37c5e57e13ece36b95cd748366ab56b7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools
    - pip
  run:
    - python >=3.9
    - numpy
    - scipy
    - matplotlib-base
    - gammapy
    - joblib
    - astropy
    - h5py

test:
  imports:
    - titrate
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/StFroese/TITRATE
  summary: asympTotic lIkelihood Tests for daRk mAtTer sEarch
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - StFroese
